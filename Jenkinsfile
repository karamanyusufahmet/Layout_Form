pipeline {
    agent any
    
    stages {
        stage('Test') {
            steps {
                echo 'Jenkins Pipeline Testi Başarılı!'
                sh 'ls -la'  // Mevcut dosyaları listeler
            }
        }
    }
}
